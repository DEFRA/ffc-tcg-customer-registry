{{- include "ffc-helm-library.container-config-map" (list . "tcg-customer-registry.config-map") -}}
{{- define "tcg-customer-registry.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
